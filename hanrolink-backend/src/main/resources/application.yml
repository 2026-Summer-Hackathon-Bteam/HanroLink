spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/hanrolink
    username: hanrolink
    password: password

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  security:
    user:
      name: admin
      password: password
