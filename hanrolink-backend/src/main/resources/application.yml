spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/hanrolink
    username: hanrolink
    password: password

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  security:
    user:
      name: admin
      password: password
